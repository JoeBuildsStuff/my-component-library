---
title: Badge
description: Displays a badge or a component that looks like a badge with multiple color variants
---

import { Badge } from "../../registry/ui/badge";
import { OpenInV0Button } from "../../registry/ui/buttons/button-open-in-v0";
import RegistryExplorer from "../../registry/registry-explorer/components/registry-explorer";
import RegistryPreview from "../../registry/registry-preview/components/registry-preview";

## Preview

<RegistryPreview>
    <div className="flex flex-col gap-6 items-center">
        <div className="flex flex-col gap-2 items-center">
            <div className="flex flex-wrap gap-2 justify-center">
                <Badge>Default</Badge>
                <Badge variant="secondary">Secondary</Badge>
                <Badge variant="destructive">Destructive</Badge>
                <Badge variant="outline">Outline</Badge>
            </div>
        </div>
        
        <div className="flex flex-col gap-2 items-center">
            <div className="flex flex-wrap gap-2 justify-center">
                <Badge variant="gray">Gray</Badge>
                <Badge variant="red">Red</Badge>
                <Badge variant="orange">Orange</Badge>
                <Badge variant="amber">Amber</Badge>
                <Badge variant="yellow">Yellow</Badge>
                <Badge variant="green">Green</Badge>
                <Badge variant="blue">Blue</Badge>
                <Badge variant="indigo">Indigo</Badge>
                <Badge variant="purple">Purple</Badge>
                <Badge variant="pink">Pink</Badge>
            </div>
        </div>
    </div>
</RegistryPreview>

## Installation

<div className="w-full flex justify-end">
    <OpenInV0Button url="https://jt-components.vercel.app/r/badge.json" />
</div>

<Tabs groupId='package-manager' persist items={['npm', 'pnpm', 'yarn', 'bun']} label='Install the badge'>

    ```bash tab="npm"
    npx shadcn@latest add https://jt-components.vercel.app/r/badge.json
    ```

    ```bash tab="pnpm"
    pnpm dlx shadcn@latest add https://jt-components.vercel.app/r/badge.json
    ```

    ```bash tab="yarn"
    yarn dlx shadcn@latest add https://jt-components.vercel.app/r/badge.json
    ```

    ```bash tab="bun"
    bunx --bun shadcn@latest add https://jt-components.vercel.app/r/badge.json
    ```
</Tabs>

## Usage

```tsx
import { Badge } from "@/components/ui/badge"
```

```tsx
<Badge>Badge</Badge>
```

## Source Code

<RegistryExplorer 
  componentName="badge" 
  defaultSelectedFile="registry/ui/badge.tsx" 
/>

## Props

<AutoTypeTable path="./registry/ui/badge.tsx" name="BadgeProps" />

## Examples

### Default Variants

The badge component comes with several built-in variants for different use cases.

<Card className="items-center justify-center min-h-[350px] bg-background shadow-none p-4">
    <div className="flex flex-wrap gap-2">
        <Badge>Default</Badge>
        <Badge variant="secondary">Secondary</Badge>
        <Badge variant="destructive">Destructive</Badge>
        <Badge variant="outline">Outline</Badge>
    </div>
</Card>

### Color Variants

A comprehensive set of color variants for different semantic meanings and visual hierarchy.

<Card className="items-center justify-center min-h-[350px] bg-background shadow-none p-4">
    <div className="flex flex-wrap gap-2">
        <Badge variant="gray">Gray</Badge>
        <Badge variant="red">Red</Badge>
        <Badge variant="orange">Orange</Badge>
        <Badge variant="amber">Amber</Badge>
        <Badge variant="yellow">Yellow</Badge>
        <Badge variant="green">Green</Badge>
        <Badge variant="blue">Blue</Badge>
        <Badge variant="indigo">Indigo</Badge>
        <Badge variant="purple">Purple</Badge>
        <Badge variant="pink">Pink</Badge>
    </div>
</Card>